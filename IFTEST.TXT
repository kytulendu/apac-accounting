	   IF WS-TEST-1 = "1"
	       MOVE 1		 TO WS-TEST
	       MOVE "X" 	 TO WS-TEST-1
	   ELSE
	       DISPLAY "TEST 1 ELSE STATEMENT" AT 0560
			WITH FOREGROUND-COLOR 1 HIGHLIGHT
			     BACKGROUND-COLOR 3
	   IF WS-TEST-2 = "2"
	       MOVE 2		 TO WS-TEST
	       IF WS-TEST-3 = "3"
		   MOVE 3	 TO WS-TEST
	       ELSE
		   DISPLAY "TEST 3 ELSE STATEMENT" AT 0760
			    WITH FOREGROUND-COLOR 3 HIGHLIGHT
	       END-IF
	       DISPLAY WS-END-3 AT 0730
		       WITH FOREGROUND-COLOR 3 HIGHLIGHT
	       CALL X"AF" USING GET-SINGLE-CHAR, KEY-STATUS
	   END-IF
	   DISPLAY WS-END-2 AT 0630
		   WITH FOREGROUND-COLOR 2 HIGHLIGHT
	   CALL X"AF" USING GET-SINGLE-CHAR, KEY-STATUS
	   IF WS-TEST-4 = "4"
	       MOVE 4		 TO WS-TEST
	       IF WS-TEST-5 = "5"
		   MOVE 5	 TO WS-TEST
	       ELSE
		   DISPLAY "TEST 5 ELSE STATEMENT" AT 0960
			    WITH FOREGROUND-COLOR 5 HIGHLIGHT
	       END-IF
	       DISPLAY WS-END-5 AT 0930
		       WITH FOREGROUND-COLOR 5 HIGHLIGHT
	       CALL X"AF" USING GET-SINGLE-CHAR, KEY-STATUS
	       IF WS-TEST-6 = "6"
		   MOVE 6	 TO WS-TEST
	       ELSE
		   DISPLAY "TEST 6 ELSE STATEMENT" AT 1060
			    WITH FOREGROUND-COLOR 6 HIGHLIGHT
	       END-IF
	       DISPLAY WS-END-6 AT 1030
		       WITH FOREGROUND-COLOR 6 HIGHLIGHT
	       CALL X"AF" USING GET-SINGLE-CHAR, KEY-STATUS
	   ELSE
	       DISPLAY "TEST 4 ELSE STATEMENT" AT 0860
			WITH FOREGROUND-COLOR 4 HIGHLIGHT
	   IF WS-TEST-7 = "7"
	       MOVE 7		 TO WS-TEST
	   END-IF
	   DISPLAY WS-END-7 AT 1130
		   WITH FOREGROUND-COLOR 7 HIGHLIGHT
	   CALL X"AF" USING GET-SINGLE-CHAR, KEY-STATUS
	   END-IF
	   DISPLAY WS-END-4 AT 0830
		   WITH FOREGROUND-COLOR 4 HIGHLIGHT
	   CALL X"AF" USING GET-SINGLE-CHAR, KEY-STATUS
	   END-IF
	   DISPLAY WS-END-1 AT 0530
		   WITH FOREGROUND-COLOR 1 HIGHLIGHT
			BACKGROUND-COLOR 3
	   CALL X"AF" USING GET-SINGLE-CHAR, KEY-STATUS.
